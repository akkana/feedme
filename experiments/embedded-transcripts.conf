[Embedded Podcast]
url = https://embedded.fm/transcripts?format=rss

# Skip the links on each line, because in Android WebView they end up
# displaying with a newline inside each parenthesized time,
# and therefore take up way too much space. Seems to be a WebView bug.
skip_pats = \([^\)]*\)

